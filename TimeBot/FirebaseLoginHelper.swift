//
//  FirebaseLoginHelper.swift
//  TimeBot
//
//  Created by QUANG on 3/13/17.
//  Copyright © 2017 QUANG INDUSTRIES. All rights reserved.
//

//import FBSDKLoginKit
/*
extension HomeVC {
    func firebaseLogin() {
        guard let accessToken = FBSDKAccessToken.current().tokenString else {
            return
        }
        let credentials = FIRFacebookAuthProvider.credential(withAccessToken: accessToken)
        FIRAuth.auth()?.signIn(with: credentials, completion: { (user, error) in
            if error != nil {
                print("Somthing is wrong with our FB user...", error!)
                return
            }
            
            print("Successfully loggin in with our user", user!)
        })
    }
}
*/
